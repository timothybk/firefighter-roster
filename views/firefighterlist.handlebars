<div class="container">
	<h3>Fire Fighters</h3>
	<div class="list-group">
		{{#each firefighter}}
			<a href="/{{this.number}}" class="list-group-item list-group-item-action">{{this.number}} {{this.rank}} {{this.name}}</a>
		{{/each}}
	</div>
</div>
